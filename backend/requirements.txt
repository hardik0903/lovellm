fastapi==0.111.0
uvicorn==0.30.1
langchain-text-splitters==0.2.2
sentence-transformers==3.0.1
ddgs==9.14.4
wikipedia==1.4.0
python-dotenv==1.0.1
chromadb==0.5.0
rank_bm25==0.2.2
pypdf==4.2.0
diskcache==5.6.3
sse-starlette==2.1.2
groq==0.9.0
python-multipart==0.0.9
pydantic==2.9.2
aiohttp==3.9.5
beautifulsoup4==4.12.3
# VLM + MaxSim architecture (Architecture 3 in rag_eval_v3)
opencv-python-headless>=4.8.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
openai>=1.0.0
numpy>=1.24.0,<2.0
# RAPTOR architecture (raptor.py) — Gaussian Mixture clustering for
# recursive abstractive tree summarization
scikit-learn>=1.3.0
# RAPTOR summarization now calls a local Ollama model instead of Groq.
# Pinned exactly (not >=) — ollama 0.4.0+ requires pydantic>=2.9, and a loose
# constraint here makes pip backtrack through every published ollama version
# one-by-one trying to satisfy that against the rest of this file.
ollama==0.6.2